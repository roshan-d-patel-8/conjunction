# conjunction

A private, passphrase-protected static page. All content in `index.html` is
AES-256-GCM encrypted (PBKDF2-SHA256 key derivation, 310k iterations) and
decrypts entirely in the browser — this repository contains only ciphertext
and the viewer shell.

Regenerated daily by an automated build; manual edits here will be overwritten.
